# Dependências só de desenvolvimento. A aplicação não importa nada daqui:
# `requirements.txt` continua sendo a lista do que roda em produção.
#
# Playwright dirige um Chromium de verdade para a validação de cada rodada
# (números lidos do DOM, capturas comparadas por SHA-256, celular em 390px).
# Depois de instalar o pacote é preciso baixar o navegador uma vez:
#
#     pip install -r requirements-dev.txt
#     playwright install chromium
#
# O navegador fica em %LOCALAPPDATA%\ms-playwright, fora do projeto.
playwright

# Pillow serviu UMA vez, na rodada 34: recortar o miolo do icone de 1024 e
# derivar os tres PNGs de `static/icones/`. Os arquivos estao versionados, e
# o comando que os gerou esta relatado na rodada - nao ha script no projeto
# que precise importar isto. Fica aqui para quem um dia refizer os icones.
#
# A aplicacao NAO importa Pillow em lugar nenhum, e ele nao entra no
# `requirements.txt` nem na imagem de producao.
Pillow
